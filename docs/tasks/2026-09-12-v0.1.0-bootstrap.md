# 建立 Anvil 樣板初版（v0.1.0，2026-09-12）
Status: done
Created: 2026-09-12
Updated: 2026-09-12
Expiry: [retired: v0.1.0]

## 目標

把研究報告第 5 節的設計草案落成可 dogfood 的 repo。
遠因與近因見 [ADR-0001](../decisions/0001-template-scope.md)。

## 裁定紀錄（使用者原話，逐字）

- 2026-09-12：「同意
並依照你的建議開始進行
專案樣板的名稱依你建議」

## 開工前查證（Pre-flight）

| 宣稱 | 查證方式 | 結果 |
|---|---|---|
| `.claude/rules/*.md` 的 `paths` frontmatter 是正式功能 | 讀 code.claude.com/docs/en/memory | 成立 |
| PreToolUse hook exit 2 會擋下該次 Bash 並把 stderr 回饋 Claude | 讀 code.claude.com/docs/en/hooks | 成立 |
| `.claude/commands/` 已與 skills 合併，新作品用 skills | 讀 code.claude.com/docs/en/skills | 成立 |
| 五個 Python 專案的一致結構是獨立收斂 | 讀 `專案架構守則.md` 與 git log | **不成立**：是 2026-07-15 統一的；守則直接當 profile |
| AI-OS 正式版在本機 | `find` 與 `gh repo list` | **不成立**：在私有 GitHub；本機兩夾是空殼與四月骨架 |

## 承接的已知限制

無（首個任務）。

## 範圍

- 根目錄：AGENTS.md、CLAUDE.md、README.md、STATUS.md、CHANGELOG.md、LICENSE、.gitignore、.editorconfig、pyproject.toml
- 佔位套件 `anvil/`
- `docs/`：ADR-0001、ADR-0002、limits.md、本任務單、四個模板、archive 說明、python_profile.md（併入守則）
- `tools/doctor.py` 十二項檢查 + `doctor_ext/` 掛點與範例
- `.claude/`：settings.json、hook、rules（docs／tools）、skills（closeout／new-task／new-adr）
- `tests/`：.gitignore 防線測試、doctor 自檢
- 全域 `~/.claude/CLAUDE.md`（六條跨專案偏好）
- `專案架構守則.md` 加退役標記；`網路範例.md` 移到 `_參考資料/`

## 明確不做

- 不推 GitHub（使用者裁定「完成就會推上 github」，由使用者決定時機）
- 不掛 XivForge（第二個套用對象，另開任務單）
- 不做 Python 以外的 profile
- 不做 `.claude/agents/`（單人先不需要專屬子代理）

## 要改的檔案

全部為 NEW，清單同「範圍」。既有檔案只動兩處：`D:\AI project\專案架構守則.md`（MODIFY：首行加退役標記）、`D:\AI project\網路範例.md`（移動）。

**不需修改的部分**：XivForge 與五個 Python 專案的任何檔案。

## 驗收條件

- [x] `python tools/doctor.py` 在 Anvil 自己身上跑，阻斷 = 0 —— 驗證：`python tools/doctor.py`
- [x] `pytest` 全過 —— 驗證：`pytest -q`
- [x] AGENTS.md ≤ 150 行 —— 驗證：doctor 的 agents 檢查
- [x] hook 腳本在模擬 stdin 下，doctor 有阻斷時 exit 2、無阻斷時 exit 0 —— 驗證：`tests/test_doctor.py::test_hook_*`
- [x] 首次 commit + tag v0.1.0 後 doctor 的 version 檢查全綠 —— 驗證：`python tools/doctor.py`

## 進度

- [x] 骨架、LICENSE、守則併入、範例搬移
- [x] 根目錄七件與佔位套件
- [x] docs：模板、ADR、limits、任務單
- [x] doctor 與 ext 掛點
- [x] .claude：settings、hook、rules、skills
- [x] tests
- [x] 全域 CLAUDE.md、守則退役標記
- [x] doctor 全綠、pytest 全過
- [x] git init、首次 commit、tag

## 收關

- ✅ 2026-09-12 收關
- 試過但無效的路徑：無（設計已在研究階段收斂，實作一次到位；研究階段被拒的方案記在 ADR-0001 選項 2～5）
- 附帶發現：L01～L05 全部在本任務建立時登錄
